# Uncomment the next line to define a global platform for your project
platform :ios, '14.0' # ✅ Moved to the top

target 'Holy places' do
  use_frameworks!

  # ✅ Add Firebase Dependencies
  pod 'Firebase/Core'
  pod 'Firebase/Firestore'
  pod 'Firebase/Auth'

  target 'Holy placesTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Holy placesUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
